// lib/models/constellation.dart

class Constellation {
  final String name; // The name of the constellation
  final List<String> stars; // List of stars or points that define the constellation

  // Constructor for creating a Constellation object
  Constellation({required this.name, required this.stars});
}
